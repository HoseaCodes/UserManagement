{
    "buildCommand": "npm install && CI=false && npm run build",
    "outputDirectory": "./build",
    "cleanUrls": true,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "server.js"
        }
    ]
}
